import { ArgTypes, Meta } from '@storybook/blocks'
import * as ThemeProviderStories from '../../../../../packages/ui-kit/src/components/ThemeProvider/ThemeProvider.stories'

<Meta title="API/ThemeProvider" of={ThemeProviderStories} />

# `ThemeProvider`

This component utilizes a `theme` prop to apply styling across the entire React component tree it encompasses. It is
ideally placed at the root of your component hierarchy to ensure comprehensive theme application

<ArgTypes of={ThemeProviderStories} sort="alpha" />
