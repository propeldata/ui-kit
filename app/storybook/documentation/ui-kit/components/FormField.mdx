import { ArgTypes, Meta } from '@storybook/blocks'
import { SourceCode } from '../../../.storybook/blocks/SourceCode'
import * as FormFieldStories from '../../../../../packages/ui-kit/src/components/FormField/FormField.stories'

<Meta title="Components/FormField/Overview" of={FormFieldStories} />

# FormField overview

Form Fields let users enter and edit text.

## Basic usage

You can import the component in your React file and use it as shown in the following example:

<SourceCode of={FormFieldStories.DefaultStory} shown />

## Props API

The following table describes the props available for the FormField component:

<ArgTypes of={FormFieldStories.DefaultStory} sort="alpha" />
