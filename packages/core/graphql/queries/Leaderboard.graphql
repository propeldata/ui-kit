query Leaderboard($uniqueName: String!, $leaderboardInput: LeaderboardInput!) {
  metricByName(uniqueName: $uniqueName) {
    leaderboard(input: $leaderboardInput) {
      headers
      rows
    }
  }
}
