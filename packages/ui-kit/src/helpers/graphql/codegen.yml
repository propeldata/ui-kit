overwrite: true
legacyMode: true
config:
  fetcher: graphql-request
generates:
  src/helpers/graphql/generated/index.ts:
    schema: src/helpers/graphql/public.graphql
    documents:
      - src/helpers/graphql/**/*.graphql
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
