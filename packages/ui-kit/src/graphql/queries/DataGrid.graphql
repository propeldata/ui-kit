query DataGrid($dataGridInput: DataGridInput!) {
  dataGrid(input: $dataGridInput) {
    headers
    rows
    pageInfo {
      hasNextPage
      hasPreviousPage
      endCursor
      startCursor
    }
  }
}
